import { TitleTrunctPipe } from './title-trunct.pipe';

describe('TitleTrunctPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleTrunctPipe();
    expect(pipe).toBeTruthy();
  });
});
